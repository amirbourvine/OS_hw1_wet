special:
continue from > for external that are simple:
idea:
#change handle1 to be not only for simple
#if cmd is external simple then cut the cmd before the >
*note: add that if there is & ignore it, in all IO/PIPE commands

#note that SIG_ALRM is with the bonus command
